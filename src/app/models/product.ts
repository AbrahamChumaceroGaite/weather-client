export interface product {
    id: number,
    name: string,
    idcategory: number,
    category: string,
    cold_hour: string,
    heat_hour: string,
    temp_optim: string,
    rain_optim: string,
    createdAt: Date,
}